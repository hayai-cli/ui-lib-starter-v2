import { defineComponent, h } from "vue"

export default defineComponent({
  name: 'EButton',
  setup(props, context) {
    // const { render } = useButton(props, context)
    return ()=>{
      return <div>4422</div>
    }
  }
})
